import { ClasseVolColorDirective } from './classe-vol-color.directive';

describe('ClasseVolColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasseVolColorDirective();
    expect(directive).toBeTruthy();
  });
});
